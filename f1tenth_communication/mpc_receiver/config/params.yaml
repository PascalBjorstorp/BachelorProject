# MPC Receiver Parameters
mpc_receiver:
  ros__parameters:
    # Path to trajectory CSV file (must match state_publisher's file)
    # Format: s_m,x_m,y_m,psi_rad,kappa_radpm,vx_mps,ax_mps2
    trajectory_file: "/home/akselmo/Documents/GitHub/BachelorProject/f1tenth_planning/trajectories/Spielberg_raceline.csv"
    
    # Input topic (MpcState from Jetson)
    input_topic: "/mpc_state"
    
    # Output topic (drive commands to VESC)
    drive_topic: "/drive"
    
    # MPC prediction horizon (number of waypoints to extract)
    horizon: 10
